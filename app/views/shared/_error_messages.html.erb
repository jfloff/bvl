<% if @volunteer.errors.any? %>
  <div id="error_explanation">
    <div class="alert alert-error">
      O registo contém <%= pluralize(@volunteer.errors.count, "erro") %>.
    </div>
    <ul>
    <% @volunteer.errors.full_messages.each do |msg| %>
      <li>* <%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>