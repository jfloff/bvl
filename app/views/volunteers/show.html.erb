<% provide(:title, "#{@volunteer.name} (#{@volunteer.username})") %>
<div class="row">
  <aside class="span4">
    <section>
      <h1>
				<%= gravatar_for @volunteer %>
				<%= @volunteer.name %>
			</h1>
    </section>
  </aside>
</div>